#!/bin/bash
# This script installs the python packages from requirements.txt

# Make sure your virtual environment is activated before running this script.
# For example: source venv/bin/activate

pip install -r requirements.txt
